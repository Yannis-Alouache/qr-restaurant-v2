meta {
  name: Delete category (manual empty category)
  type: http
  seq: 92
  tags: [
    admin
    manual
    destructive
  ]
}

delete {
  url: {{baseUrl}}/api/admin/categories/{{categoryId}}
  auth: none
}

