meta {
  name: Create side item
  type: http
  seq: 7
  tags: [
    admin
    smoke
  ]
}

post {
  url: {{baseUrl}}/api/admin/menu-items
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}


body:json {
  {
    "categoryId": "{{categoryId}}",
    "name": "Frites maison",
    "price": 4.20
  }
}

script:post-response {
  const body = res.getBody();

  if (res.getStatus() === 201) {
    bru.setEnvVar("sideItemId", body.id);
  }
}
